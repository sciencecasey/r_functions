setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile("tic_tac_toe_AI_minimax.R", line = -1L, column = -1L)
}, action = "append")


setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile("Project2_writeup.Rmd", line = -1L, column = -1L)
}, action = "append")

setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile("README.txt", line = -1L, column = -1L)
}, action = "append")
