setHook("rstudio.sessionInit", function(newSession) {
  if (newSession)
    rstudioapi::navigateToFile("code/main.R", line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
  if (newSession)
    rstudioapi::navigateToFile("code/proj_1_part_1_iris.R", line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
  if (newSession)
    rstudioapi::navigateToFile("code/proj_1_part_1_mahalanobis_outlier.R", line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
  if (newSession)
    rstudioapi::navigateToFile("code/proj1_part1_feature_ranking.R", line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
  if (newSession)
    rstudioapi::navigateToFile("code/ticTacToe.R", line = -1L, column = -1L)
}, action = "append")


