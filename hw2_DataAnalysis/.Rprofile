
setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile('code/2.1_testStatistics.R', line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile('code/2.2_syntheticData.R', line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile('code/3_dataTraining.R', line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile('code/4_dct_pca_numbers.R', line = -1L, column = -1L)
}, action = "append")
setHook("rstudio.sessionInit", function(newSession) {
    if (newSession)
        rstudioapi::navigateToFile('code/more_4_dct_pca.R', line = -1L, column = -1L)
}, action = "append")
